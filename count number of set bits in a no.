#include <stdio.h>

int main() {

  int n,i,count=0;
  printf("Enter a number");
  scanf("%d",&n);
  for (i=0;i<32;i++)
  {
      if((n>>i)&1)
      count++;
  }
  printf("the no. of set bits is %d",count);

}
